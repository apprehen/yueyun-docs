/**
 * 最长回文子串
 * @param {string} s
 * @return {string}
 * */
// 满足条件：1.最长 2.回文
var longestPalindrome = function(s) {
  const len = s.length
  if (len < 2) return s
  // 滑动窗口
  let left = 0, right = 0
  for (let i = 0; i < len; i++) { 
    
  }
};